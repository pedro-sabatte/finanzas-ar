// Copiá este archivo como config.local.js y completá los valores.
// config.local.js está en .gitignore — nunca se sube al repo.
const CONFIG = {
  APPS_SCRIPT_URL: 'https://script.google.com/macros/s/TU_DEPLOYMENT_ID/exec',
  API_TOKEN: 'tu-token-secreto-aqui'
};
